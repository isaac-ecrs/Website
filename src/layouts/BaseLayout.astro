---
/**
 * Base Layout
 *
 * This wraps all pages with common elements: header, footer, and global styles.
 *
 * WHY padding-top: 120px on <main>?
 * TopHeader + Header are position: fixed, so they overlay the page content.
 * Without padding, the first element on every page would render behind the
 * headers. 120px accounts for both headers' combined height.
 *
 * WHY Netlify Identity widget in <head>?
 * Decap CMS uses Netlify Identity for authentication. The widget must load
 * on ALL pages (not just /admin/) because:
 * 1. OAuth login redirects back to the main site after authentication
 * 2. The widget intercepts the redirect and completes the login flow
 * Without this, users would see a blank page after logging in.
 */
import TopHeader from '../components/TopHeader.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ScrollToTop from '../components/ScrollToTop.astro';
import '../styles/global.scss';
import '@fortawesome/fontawesome-free/css/all.min.css';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'ECRS - Come to Play. Play to Learn' } =
  Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title} | ECRS</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <!--
      WHY: Netlify Identity widget handles OAuth callbacks from Decap CMS login.
      Must be on all pages since OAuth redirects to the main site, not /admin/.
    -->
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"
    ></script>
  </head>
  <body>
    <TopHeader />
    <Header />
    <!-- WHY 120px: Offsets content below the fixed TopHeader + Header -->
    <main style="padding-top: 120px;">
      <slot />
    </main>
    <Footer />
    <ScrollToTop />
  </body>
</html>

<script>
  import '../scripts/scroll-reveal';
</script>

<style>
  body {
    margin-top: 0;
  }
</style>
