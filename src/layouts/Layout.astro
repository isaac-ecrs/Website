---
import '~/assets/styles/tailwind.css';

import { I18N, SITE } from 'astrowind:config';

import CommonMeta from '~/components/common/CommonMeta.astro';
import Favicons from '~/components/Favicons.astro';
import CustomStyles from '~/components/CustomStyles.astro';
import ApplyColorMode from '~/components/common/ApplyColorMode.astro';
import Metadata from '~/components/common/Metadata.astro';
import SiteVerification from '~/components/common/SiteVerification.astro';
import Analytics from '~/components/common/Analytics.astro';
import CloudflareWebAnalytics from '~/components/common/CloudflareWebAnalytics.astro';
import EventTracker from '~/components/common/EventTracker.astro';
import MetaPixel from '~/components/common/MetaPixel.astro';
import ConsentBanner from '~/components/common/ConsentBanner.astro';
import BasicScripts from '~/components/common/BasicScripts.astro';

// Comment the line below to disable View Transitions
import { ClientRouter } from 'astro:transitions';

import type { MetaData as MetaDataType } from '~/types';
import siteData from '~/data/settings/site.json';

export interface Props {
  metadata?: MetaDataType;
}

const { metadata = {} } = Astro.props;
const { language, textDirection } = I18N;
---

<!doctype html>
<html lang={language} dir={textDirection} class="2xl:text-[20px]">
  <head>
    <CommonMeta />
    <Favicons />
    <CustomStyles />
    <ApplyColorMode />
    <Metadata {...metadata} />
    <SiteVerification />
    <Analytics />
    <CloudflareWebAnalytics />
    <EventTracker />
    <MetaPixel />

    <link rel="preconnect" href="https://images.unsplash.com" />
    <link rel="preload" href="/fonts/rusty-hooks.woff2" as="font" type="font/woff2" crossorigin />

    <script
      type="application/ld+json"
      set:html={JSON.stringify({
        '@context': 'https://schema.org',
        '@type': 'NGO',
        name: 'Eastern Cooperative Recreation School',
        alternateName: siteData.name,
        url: Astro.site ? String(Astro.site) : SITE.site,
        logo: Astro.site ? String(new URL('/images/ecrs-logo.png', Astro.site)) : '/images/ecrs-logo.png',
        description: siteData.mission,
        sameAs: siteData.socialLinks.map((s) => s.url).filter(Boolean),
      })}
    />

    <slot name="head" />

    <!-- Comment the line below to disable View Transitions -->
    <ClientRouter fallback="swap" />
  </head>

  <body class="antialiased text-default bg-page tracking-tight">
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:rounded focus:bg-primary dark:focus:bg-[#bd0f5d] focus:text-white focus:font-semibold focus:shadow-lg"
    >
      Skip to main content
    </a>
    <slot />

    <ConsentBanner />
    <BasicScripts />
  </body>
</html>
