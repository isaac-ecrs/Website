---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

interface Props {
  icon?: ImageMetadata;
  image?: ImageMetadata;
  title: string;
  description: string;
  link?: string;
  linkText?: string;
}

const { icon, image, title, description, link, linkText = 'Learn More' } = Astro.props;
---

<div class="feature-card animate-fade-bottom">
  {image && (
    <div class="feature-image">
      <Image src={image} alt={title} />
    </div>
  )}
  {icon && !image && (
    <div class="feature-icon">
      <Image src={icon} alt={title} />
    </div>
  )}
  <h3 class="feature-title">{title}</h3>
  <p class="feature-description">{description}</p>
  {link && (
    <a href={link} class="feature-link">{linkText} →</a>
  )}
</div>

<style lang="scss">
@use '../styles/variables' as *;
@use '../styles/mixins' as *;

.feature-card {
  background: white;
  padding: 2em;
  border-radius: $border-radius-card;
  box-shadow: $shadow-image;
  transition: transform $transition-fast, box-shadow $transition-fast;

  &:hover {
    transform: translateY(-5px);
    box-shadow: $shadow-deep;
  }

  .feature-image {
    width: 100%;
    height: 200px;
    border-radius: $border-radius-card $border-radius-card 0 0;
    overflow: hidden;
    margin: -2em -2em 1.5em -2em;

    img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
  }

  .feature-icon {
    width: 100px;
    height: 100px;
    border-radius: $border-radius-circle;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5em;
    background-color: $color-accent;

    img {
      width: 60%;
      height: 60%;
      object-fit: contain;
    }
  }

  .feature-title {
    color: $color-primary;
    margin-bottom: 0.75em;
    font-size: 24px;
  }

  .feature-description {
    color: $color-text-dark;
    margin-bottom: 1em;
  }

  .feature-link {
    display: inline-block;
    color: $color-accent;
    font-weight: 600;
    text-decoration: none;

    &:hover {
      color: $color-teal;
    }
  }
}
</style>
